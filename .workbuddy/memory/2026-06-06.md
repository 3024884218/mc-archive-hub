# 2026-06-06 工作日志

## 项目探索
- 首次浏览 mc-archive-hub 项目，完成全面代码审查
- 项目定位：Minecraft 存档分享平台（Spring Boot 3.3.5 + Java 17 + SQLite + 原生前端 SPA）
- 34个Java源文件，6个前端文件（index.html + 5个JS + 1个CSS）
- 功能完整：用户认证、存档管理、社交互动、通知、打赏

## UI 重设计 (impeccable + design-taste-frontend)
- 全面重写 style.css：OKLCH 色域、暗色优先、"方块建筑"美学
- 修复 impeccable 违规：渐变文字、卡片图片缩放、侧边条 Toast、em-dash
- 修复 taste-skill 违规：Hero metric 模板、badge pulse dot、hero stats
- Hero 重设计：移除统计数字模板，改为内嵌字幕
- 颜色体系：oklch(0.52 0.185 145) 主绿 + oklch(0.1 0.005 160) 深底
- 卡片 hover：改 border/box-shadow 变化代替图片缩放
- Toast：用全边框 + 背景色区分代替 left-border stripe
- 所有 em-dash 替换为 middle dot
- 版本号升至 v3/v4，缓存 bust

## Mod 列表功能
- Archive 新增 modsJson 字段（JSON TEXT），存储 [{name, url}] 数组
- 上传/编辑表单增加 Mod 输入区（名称 + 下载链接，支持动态增删行）
- 存档卡片显示 Mod 数量金色角标
- 详情页渲染 Mod 列表标签（有链接可点击跳转）
- 后端全套适配（DTO + Service + Controller）

## 外部下载链接功能
- Archive 新增 downloadUrl 字段（500字）
- 上传表单增加"外部下载链接"输入框（提示填写网盘等备用链接）
- 编辑表单同步支持
- 详情页有外链时在操作栏显示金色"外部下载"按钮（新窗口打开）

## 代码审查 & 安全修复
- CSS 注入修复：avatarUrl 在 background-image:url() 中加引号并 escape（app.js L1384,1443,1240）
- Meta 加载空 catch → 默认空对象兜底，防止后续 undefined.mcVersions crash
- MC.init() 添加 .catch() 处理初始化失败
- renderProfile 空 catch 后增加 currentUser 空值判断，防止 undefined.email crash
- em-dash 残留清理（api.js, enhance.js 注释）
- 低风险保留：btn:active scale(0.98) 为正确触觉反馈，nav border-left 为布局用途

## 登录问题修复
- **SMTP 未配置时自动激活邮箱**：register 时检测 emailConfigured，false 则直接 setEmailVerified=true
- **SMTP 未配置时跳过登录邮箱拦截**：login 时增加 isEmailConfigured() 条件判断
- **前端登录不再校验密码长度**：8位限制仅保留在注册侧（后端无此限制，旧用户短密码被误拦截）
- EmailService 新增 isEmailConfigured() public getter

## Mod 文件上传功能
- 每个 Mod 行新增 📦 按钮，点击上传 .jar/.zip 文件（最多 50MB）
- 选中文件后按钮变绿色 ✅，鼠标悬停显示文件名
- 后端 CreateArchiveRequest 新增 modFiles 字段
- FileStorageService 新增 storeModFile（白名单 .jar/.zip）
- ArchiveService 新增 injectModFilePaths（将存储路径注入 modsJson）
- 详情页有 filePath 的 Mod 显示绿色 ⬇ 下载按钮
- ArchiveController 新增 /{id}/mod-download 端点
